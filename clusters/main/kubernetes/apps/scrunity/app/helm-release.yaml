apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: scrunity
  namespace: scrunity
spec:
  interval: 15m
  chart:
    spec:
      chart: scrunity
      version: 14.8.0
      sourceRef:
        kind: HelmRepository
        name: truecharts
        namespace: flux-system
  releaseName: scrunity
  values:
